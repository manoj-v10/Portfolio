# public/

- `Manoj_V_FullStack_Developer_Resume.pdf` — linked from the hero résumé tile
  (configured as `profile.resumeHref` in `src/config/portfolioData.ts`).

Optional additions:

- `og-image.png` (1200×630) — then add `openGraph.images` in `src/app/layout.tsx`.
- `favicon.ico` — Next.js picks it up automatically from `src/app/` or `public/`.
